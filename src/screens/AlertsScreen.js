import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AlertsScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Alertas</Text>
      {/* Lista de alertas passados e presentes */}
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent }
})